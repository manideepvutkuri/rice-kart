import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.fyben.shop',
  appName: 'com.fyben.shop',
  webDir: 'dist/myapp/browser'
};

export default config;
